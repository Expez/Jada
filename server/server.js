// Meteor.publish("log", function () {
//   return Meteor.log.find({id: this.userid});
// });

// Meteor.publish("foods", function () {
//   return Meteor.foods.find({id: this.userid});
// });

// Meteor.publish("macros", function () {
//   return Meteor.macros.find({id: this.userid});
// });

Meteor.startup(function () {
  // code to run on server at startup
});
